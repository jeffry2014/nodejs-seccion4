# Notas:
Este es mi primer programa en nodejs
```
Options:
      --help     Show help                                             [boolean]       
      --version  Show version number                                   [boolean]       
  -b, --base     Es el numero base de la tabla de multiplicar[number] [required]       
  -l, --listar   Indicador para determinar si se debe mostrar o no la tabla en
                 consola.                             [boolean] [default: false]  
```